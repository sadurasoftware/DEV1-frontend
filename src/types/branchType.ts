export type branchType = {
    name: String,
    pincode:Number,
    locationId: Number,
    stateId: Number,
    countryId: Number
}

export type updateBranchType = {
    id: Number,
    name: String,
    pincode:Number,
    locationId: Number,
    stateId: Number,
    countryId: Number
}
